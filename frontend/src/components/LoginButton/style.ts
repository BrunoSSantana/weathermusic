import styled from "styled-components";

export const Container = styled.a`
  outline: none;
  text-decoration: none;

  position: absolute;
  top: 50vh;
  right: 50px;

  &:hover {
    cursor: pointer;
  }
`